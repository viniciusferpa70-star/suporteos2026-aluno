# Apenas no banco descartável de referência do PETSHOP.
spring.datasource.url=jdbc:postgresql://localhost:5432/petshop2026_reference
spring.datasource.username=${DB_REFERENCE_USERNAME:suporteos_app}
spring.datasource.password=${DB_REFERENCE_PASSWORD}
spring.main.web-application-type=none
spring.liquibase.enabled=false
spring.jpa.hibernate.ddl-auto=create
